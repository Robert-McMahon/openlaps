# Dashboard documentation moved

Dashboard authoring and provisioning rules are documented at
[`docs/operations/dashboards.md`](../../../../docs/operations/dashboards.md).

The JSON files in this directory remain the source of truth. Grafana is
provisioned read-only; browser-only edits are not durable.
