# NATS documentation moved

NATS topology, TLS setup, listener exposure and rotation are documented at
[`docs/operations/nats-security.md`](../../docs/operations/nats-security.md).

The checked-in server configuration remains in this directory. Never place
certificates, keys, credentials or deployment `.env` files here.
