# Vehicle target documentation moved

Target concepts, supported hardware and the process for adding a board are in
[`docs/hardware/`](../../docs/hardware/index.md).

Keep each target's `hardware.yaml`, `target.env`, encoder configuration and
optional Compose overlay beside one another in this directory.
